/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["lh3.googleusercontent.com", "localhost", "storage.googleapis.com", 'mecha-mayhem-frontend.vercel.app'],
    }
};

export default nextConfig;
